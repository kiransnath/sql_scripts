--Concat and Substr to append Occupation type after name. eg. Ash(D) for Doctor

select name||'('||substr(occupation,1,1)||')'
from occupations
order by name ;
select 'There are a total of '||count(*)||' '||lower(occupation)||'s.' from occupations
group by occupation
order by 1;
